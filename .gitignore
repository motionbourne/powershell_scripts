# This repo publishes ONLY the standalone HTML tools.
# Everything else stays local and is ignored: PowerShell/bash scripts, docs,
# generated audit output, BitLocker key files, and local Older/ backups.
*
!.gitignore
!*.html
